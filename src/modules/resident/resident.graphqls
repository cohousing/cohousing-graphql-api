type Resident {
    id: Int!
    name: String!
    username: String
    home: Home
}

extend type Query {
    resident(id: Int!): Resident
    allResidents: [Resident!]

    loggedInResident: Resident
}