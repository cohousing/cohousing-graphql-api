type User {
    id: Int!
    username: String!
    resident: Resident
    superAdmin: Boolean!
}

extend type Query {
    allUsers: [User!]!
    user(id: Int!): User
}

extend type Mutation {
    createUser(username:String!, password:String!, superAdmin: Boolean!, residentId: Int): User!
}
