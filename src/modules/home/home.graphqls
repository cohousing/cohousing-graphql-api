# A physical space, which can have residents. Normally a house, an apartment or a room,
# but it can be any kind of structure representing something the individual residents
# own.
type Home {
    id: Int!
    name: String!
    residents: [Resident!]
}

extend type Query {
    home(id: Int!): Home
    allHomes: [Home!]
}